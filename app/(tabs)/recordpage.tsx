import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import Camera from '@/features/record/record'

const Record = () => {
  return (
    
        <Camera/>
   
  )
}

export default Record